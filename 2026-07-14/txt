


windows : 192.168.2.232
linux : 192.168.2.254
	vncviewer 192.168.2.254
/tools
	vnc-4.1.2-14.el5_3.1.i386.rpm

	rpm -ivh vnc-4.1.2-14.el5_3.1.i386.rpm

=====================================================
函数指针：
	注意两点：
		1 函数返回类型要跟函数保持一致
		2 函数参数也要保持一致
			三一致原则：类型 个数 位置
指针函数：
	注意：
		不可以返回非静态局部变量的地址

=================================================
结构体：
	自定义类型
	结构体数组
	结果体指针
	作为函数参数
		值传递
		地址传递
	柔性数组
	结构体大小
		
	结构体位操作

共用体：
	区别：结构体成员空间是独立的
	     共用体的每一个成员都是空间内存空间，空间大小由最大成员所占的空间大小决定。

枚举： 枚举常量
==================================================
大端： 高低 低高
小端： 高高 低低

============================================
预处理：
	伪代码和特殊字符
	伪代码：就是#开头
		头文件:
			1 <> 和 "" 区别？
				系统头文件存放目录：/usr/include
				pwd  => /usr/include
			2 防止头文件重复包含语句
				#ifndef __FILENAME_H__
				#define __FILENAME_H__


				#endif 

		宏命令:宏常量
			纯文本替换
		format:
			#define macro_name macro_value
			
		宏和函数区别
			1 宏是在编译的时候处理  
			  函数是在运行的时候调用

			  宏的执行效率比函数要高
			  空间效率比函数要低
			2 函数比宏要安全
				通用性宏不函数要高
			3 函数代码如果少于5行，则可以用宏实现
				宏是一行实现

			4 宏传递参数不需要类型

		练习：
			宏实现求两各个数最大数

		练习：
			GETLINES("input string", buf);
			
			printfgets
		条件编译 ：
			
	特殊字符：
		__LINE__ ...

======================================
#	:	表示字符串连接符
##	:	表示字符连接符

条件编译
	格式一
		#if const
			body
		#enddif
	const常量如果是零，则条件为假，不执行中间语句
		如果const是非零，则执行

	格式二：
		#if macro_name
			body
		#endif

	格式三：
		#if macro_name
			cmd1
		#elif macro_name
			cmd2
		...
		#else
			cmd_other
		#endif

	格式四
		#ifdef  

		#endif

	特殊字符


==========================================================
file IO
	1 打开文件
		FILE *fopen(const char *path, const char *mode);
		第一个参数：表示文件路径
			绝对路径： /
			相对路径： pwd
		第二个参数：表示文件模式
			r :表示只读
				如果文件不存在，则打开失败
				如果文件存在， 从文件开头读
			r+:表示可读可写
				文件不存在，则打开失败
				文件存在，则覆盖的方式写文件
			w : 表示只写
				文件不存在，则创建文件
				文件存在，则清空的写
			w+: 表示可读可写
				文件不存在，创建
				文件存在，清空
			a :表示追加写
				文件不存在，创建
				文件存在，则写到结尾
			a+:表述可读可写的追加
				文件不存在，则创建
				文件存在：
					写：文件结尾
					读：文件开头
		返回值：
			成功：有效指针变量
			失败：NULL
		err:
			fopen("./test", r)
			fopen("./test", 'r')
		"r"	


		练习：
			实现cp
			cp srcfilename destfilename
				srcfilename => r => data => destfile => w
	2 关闭文件
		int fclose(FILE *fp);


	3 获取文件内容
		int fgetc(FILE *stream);

	4 写入数据到文件中
		int fputc(int c, FILE *stream);


按行读和写
	char *fgets(char *s, int size, FILE *stream);
	失败：NULL

	写数据
	int fputs(const char *s, FILE *stream);


========================================================

homework:
	实现grep  : 查看匹配的关键

	[root@www 2026-07-14]# grep root ./passwd 
	root:x:0:0:root:/root:/bin/bash
	operator:x:11:0:operator:/root:/sbin/nologin
	
	1 显示行号
	2 关键子高亮显示










windows : 172.18.71.208






1 退出条件
	break
	continue
2 延迟函数
	sleep
	usleep

随机数：
	rand() 

	srand(time(NULL));
	or
	srand(getpid());
	
goto :
	1 深层退出
	2 循环
	3 做容错宏

----------------------------
函数：
	C语言最小的编程单位
	就是把通用性的代码封装处理，以供其他用户调用

分类：
	用户角度：
		1 库函数：
			就是标准C库给用户提供的一组功能函数
			printf scanf getchar putchar ...
			rand() srand() ...
			[root@www 2026-05-30]# ls /lib/libc.so.6 
			/lib/libc.so.6

		2 自定义函数：
			有用户根据客户需求实现功能函数接口

函数组成：
	由四部分组成

	1 函数类型	：
		char short int long float double long double  7种
		是由函数返回值类决定

		如果函数没有返回值，则函数类型 void
	2 函数名	：函数名表示函数的入口地址
		根定义变量名的规则是一样的。
		函数起名一定要形象
	3 函数参数	：(arg)
		format: arg_type arg_name, arg_type arg_name, ...
		函数参数对函数功能扩张
		
		如果不接受任何参数 => void
	4 函数体	： {}
		函数功能的实现


	int main(void)
	{
		return 0;
	}
	

========================
分类：
	是否传参：
	有参函数：
		实参：传递函数参数 | 调用函数参数
		形参：接受函数参数 | 实现函数参数
	无参函数：
		无参：void

===============================
练习：
	实现两个数交换
	swap()实现两个数交换
	？
	如果没有交换成功，分析原因


函数传参：
	1 值传递 不会改变原来数据的内容
	2 地址传递 则会修改原来的数据内容

函数参数传递遵循原则：
	一般情况：
		参数个数，类型，顺序要保持一致

特殊情况：
	...
	printf("hello world!\n");
	printf("val : %d\n", val);
	printf("a : %d b : %d\n", a, b);

	#include <stdarg.h>

	//初始化
	void va_start(va_list ap, last); 
	第一个参数：表示用于存储不定参列表
	第二个参数：表示最后有名参数

	//用于访问数据成员
	type va_arg(va_list ap, type);
	第一个参数：存储数据空间列表
	第二个参数：表示类型

	//销毁
	void va_end(va_list ap);

	//拷贝
	void va_copy(va_list dest, va_list src);

练习：
	add(12, 34)
	add(12,34,56);
	add(12,34,56,78,90);

==============================
函数参数形式：
	

========================
递归函数：
	分类：
		直接递归：就是函数调用自己
			A => A
		间接递归：就是函数之间无论调用了多少层，最终有调用到自己
			A => B => .... => A
注意：
	1 使用递归函数一定要有退出条件
		如果没有，则一定会导致段错误。
==========================================
分文件：
	
	系统头文件存放路径：/usr/include


头文件包含方式：
	1 <>  表示从系统头文件的目录中查找(/usr/include)
	2 ""  表示从当前工作目录中查找，如果当前的工作目录中没有，则从系统头文件的目录中查找

	-I : 表示告诉编译器头文件存放路径

================================
	gcc file.c ... =》 a.out

一个源文件生成二进制可执行文件步骤
	1 ： 预处理 .i
		处于伪代码和特殊字符
		伪代码：#开头的
			头文件
			宏命令
			条件编译
		gcc -E file.c -o file.i

		就是把伪代码和特殊字符展开存放到源文件中

	2 ： 编译 .s
		用于检查语法规则和词法错误

		gcc -S file.i -o file.s
	3 ： 汇编 .o
		把汇编代码生成目标代码
		gcc -c file.s -o file.o
	4 ： 链接
		gcc file.o -o file
		链接库文件(.so .a)


======================================
homework:
	1 实现汉诺塔
		
	2 实现菲波那契数列
		
	3 走台阶
		9 => 1 2 3 问有多少中走法

	4 封装四种三角形
		
	5 封装黑色星期五 => 三天大于两晒网	 
		int count_year_days(int sy, int ey)
		int count_month_days(int month, int year);
		int is_year(int year);
	
	 => file

		











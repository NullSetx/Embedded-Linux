


指针函数：
	首先是函数，再是指针，说明这个函数的放回类型是指针类型
	注意：不可以返回非静态局部变量的地址

函数指针：
	首先是指针，再是函数，说明这个指针指向的是一个函数名

	注意：
		1 函数返回类型要一致
		2 函数参数保持三一致原则
			类型 个数 位置

	练习：
		函数指针 => void swap(int *, int *)
		并可以调用成功

	练习：
		+ - * / % ^
		(5 option 3) option 2 == 4
		(5 + 3) / 2 = 0
		统计有满足条件的数量

=============================================
结构体： 自定义类型
	把不同的数据类型保存到同一片内存空间中


格式：
	struct tyep_name{
		//number list
		type val_name;
	};


练习： 通过键盘接受一个学生信息，并保存到cls结构体中
==========================================================
1 定义结构体类型
2 定义结构体变量
3 初始化，访问， 修改
4 结构数组 结构体指针 
5 结构申请内存空间
6 柔性数组
7 结构体大小
8 结构位操作
9 结构体参数
10 结构嵌套

=================================
共用体：
	共用体的每一个成员共用一片内存空间

homework1:
	利用共用体检测本机是大端模式还是小端模式？？？？
	大端模式：低字节存储高地址 高字节存储低地址
	小端模式：低字节存储低地址 高字节存储高地址
homework2:
	结构体实现表达式匹配

homework3:
	弹球程序 => 多球 => 求碰到球反弹

homwwork4:
	实现学生结构添加 删除 查找 修改 












